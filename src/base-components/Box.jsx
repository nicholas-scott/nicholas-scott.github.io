import styled from "styled-components"
import {
  space,
  layout,
  border,
  color,
  flexbox,
  grid,
  position,
  background,
} from "styled-system"

const Box = styled.div`
  ${space}
  ${layout}
  ${color}
  ${flexbox}
  ${border}
  ${grid}
  ${position}
  ${background}
`

export default Box
